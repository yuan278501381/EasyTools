# EasyTools 版本更新日志 (Changelog)

---

## 🌟 v1.0.4 官方正式版发布说明

### 1. 智能文件对话框 (Dialog Enhancer) —— 零打扰、智能记忆与丝滑吸附
- **专属独立记忆，再次打开瞬间直达**：每个常用软件（如 VS Code、浏览器、办公套件、设计软件等）在打开或保存文件时，都能自动记住各自上一次实际确认使用的文件夹。再次触发文件弹窗时直接定位到目标目录，彻底告别在层层深文件夹中反复翻找；
- **智能防误记与父目录提炼**：只有在用户真正点击“打开”或“保存”确认后才会记录路径（取消或关闭窗口不会误记）；打开具体文件时精准提取所在目录，选择文件夹时停留在同级目标目录，路径定位永远符合直觉；
- **快捷悬浮胶囊生命周期强绑定**：顶部快捷悬浮工具栏（最近使用、与当前资源管理器同频、工作区置顶）仅在文件对话框处于前台时优雅呈现，切换到其他软件时自动隐退，彻底杜绝悬浮窗孤立残留或遮挡其他操作；
- **系统提示弹窗零误伤与原生兼容**：彻底消除对普通系统提示弹窗（如保存确认、错误提醒、MessageBox）的误判与干预，确保所有软件的原生弹窗 100% 稳定响应。

### 2. 全盘极速搜索 (Everything Instant Search) —— 灵动跟手与打字生理节奏自适应
- **人类打字生理节奏动态防抖**：在快速连续输入关键词时（如急促键入字母或拼音），搜索框智能感知打字节奏并无感合并查询请求；在停顿思索时（约 120ms）瞬间触发极速检索，打字手感极度轻快跟手，告别频繁检索带来的微卡顿与画面闪烁；
- **大体量全文检索前置保护**：当输入前缀较短时自动保护全文深层扫描，输入达到有效特征长度后才触发深度内容索引，检索效率大幅提升且不挤占后台资源。

### 3. 屏幕按键回显 (Keycast Overlay) —— 黄金字号与晶体半透明质感
- **默认 36px 黄金字号视觉呈现**：全新优化按键回显的基准字号与键帽间距，在大屏、高分屏及日常录课直播中呈现出恰到好处的饱满清晰度；
- **85% 默认晶体半透明质感**：优化整体不透明度渲染通道与初始参数，既能清晰醒目呈现按键组合，又保证背景内容若隐若现，不遮挡工作区核心内容。

### 4. 托盘控制中心与全局 UI 质感 (Tray & Unified UI)
- **220px 黄金紧凑宽度托盘菜单**：重调托盘控制中心弹窗宽度与弹性布局，右键菜单呼吸感更充裕，配置修改后的重启提示横幅自适应排版更舒适；
- **微晶数字步进控件 (Micro-crystal Stepper)**：全面替换设置界面中粗糙的原生数字输入框，带来带有微晶渐变、加减微按钮、平滑增减与防溢出边界的世界级数字调节体验。

---

## 🌟 v1.0.3 官方正式版发布说明

### 1. 自由定制：安装向导 6 大功能模块按需选装 (Modular Installer)
- **纯粹自如，告别冗余**：全新安装向导支持在安装初始界面自由勾选 6 大核心模块（截屏标注、极速搜索、鼠标特效、按键回显、鼠标手势、对话框增强），按需定制专属轻量工具箱；
- **安装即用，偏好无缝继承**：首次启动自动同步你的模块偏好，无需在安装后再次逐项手动配置；
- **高分屏像素级对齐**：安装向导全面优化 4K / 2K 高分屏缩放适配，彻底消除多选框文字挤压与边缘截断。

### 2. 屏幕按键回显：黄金微晶实体键帽与智能防打扰 (Keycast Masterpiece)
- **1:1 实体微晶键帽美学**：全新 33px 紧凑外壳、1.45:1 优雅微晶底座与 Windows 11 微透视现代矢量微标，极具桌面现代质感；
- **打字智能静默，告别疯狂弹窗**：重构键盘状态机与 Shift 打字过滤算法，常规码字、写代码及中英文切换时自动智能静默，唯有快捷键操作才优雅呈现；
- **自由配色与智能自适应对比度**：修饰键底色支持自适应时下「主题色」，文字、背景与不透明度全面开放自定义，智能反差算法确保无论桌面壁纸深浅始终字字饱满锐利；
- **独立功能键 (F1~F12) 专属控制**：新增独立功能键细粒度开关，默认出现位置优化为视觉干扰最小的屏幕左上角。

### 3. 界面与排版：统一 3D 物理胶囊开关与全链路清晰字体 (Unified UI & Typography)
- **触觉级 3D 物理胶囊开关**：重塑全系统所有设置开关，具备微米级立体阴影、物理回弹动效与主题色流光渐变；
- **ClearType 次像素饱满字型**：建立统一应用级字体栈，支持界面字体智能选择与降级回退，彻底告别界面文字发虚、单薄；
- **微晶代码胶囊 (CodeBadge)**：窗口类名、进程名与文件路径全部包裹在精致的微晶等宽胶囊中，信息读取更清晰。

### 4. 极速全盘搜索：多关键词穿透与丝滑连打 (Search Precision & Penetration)
- **连续输入不再漏词**：彻底修复快速连续打字时光标收缩导致的多词跨路径文件无法匹配问题，搜索精准度与命中率大幅提升；
- **启动秒级响应**：优化搜索窗口预热调度与渲染就绪保护，呼出搜索框零闪烁、零失焦。

### 5. 沉浸式窗口交互与原生体验 (Window Navigation)
- **双击标题栏无级缩放**：设置等无边框窗口全面支持双击标题栏最大化与平滑还原；
- **原生右键菜单精准呼出**：标题栏右键物理光标绝对坐标定位，唤出 Windows 原生系统菜单；
- **全系统 151 个配置项 100% 稳定落盘**：底层 IPC 协议与持久化全覆盖加固，确保每一项设置改动都能实时保存生效。

---

## 🌟 v1.0.2 官方正式版发布说明

### 1. 鼠标演示与特效 (Spotlight & Mouse Effects) —— 演示与教学的视觉利器
- **摇晃鼠标秒级唤醒，三大全屏聚焦动效直击视线**：
  - **「向心引力折叠」 (默认推荐)**：快速摇晃鼠标或按下快捷键，全屏双道高能光环高速向鼠标圆心收拢，伴随物理弹性微震与电影级深邃暗角，瞬间将全场观众或学生注意力牢牢锁死在鼠标光标处；
  - **「科技声纳雷达」**：光标原点向外激荡 3 道声纳脉冲光波，4 段战术 HUD 弧线刻度微旋磁吸锁定，极客科技感拉满；
  - **「极简极光涟漪」**：柔和贝塞尔暗角平滑浸润，光圈外缘溢出单道温和极光涟漪，舒适护眼不刺眼；
  - **更大更清晰的光圈**：聚光灯默认聚焦直径提升至 300px，即使在 4K 超高清大屏上也能清晰直观。
- **9×9 大师级动效矩阵**：
  - 点击鼠标时，提供星芒微粒迸发、彩色声纳微环、量子引力、特斯拉电弧、宣纸水墨、CAD 标尺、晨露气泡、超新星等 9 种灵动点击动效；
  - 移动鼠标时，提供流星拖尾、霓虹光线、彗星微粒等 9 种轨迹动效，拖动光标如行云流水。
- **120Hz/60Hz 丝滑无抖动**：接入高精度多媒体定时器管线，彻底消除动画卡顿与掉帧。
- **彻底根治免打扰铃铛误弹**：通过微型局部视口（100~300px）与 1px 物理几何避让，彻底根治此前 Windows 误判全屏而频繁在右下角弹出 `🔔z 专注助手（免打扰）` 铃铛的问题，且 GPU 显存与渲染开销骤降 99%。

### 2. 屏幕按键回显 (Keycast Overlay) —— 晶体键帽与极致清晰
- **半透明底色下，文字依然 100% 清晰锐利**：彻底解耦按键背景透明度与文字/图标渲染，就算把键帽背景调到半透明磨砂，按键文字与徽标依然实心饱满、字字分明，杜绝文字发虚发淡；
- **智能自适应对比度 (WCAG 2.2 AAA 级)**：内置智能亮度感知算法，在自动模式下遇到深色或半透明底色，自动匹配纯白高对比度文字，无论桌面壁纸如何变幻，始终一目了然；
- **物理晶体键帽设计**：重新调校所有按键的晶体键帽排版，彻底消除桌面文字透视穿帮与边缘溢出；
- **全面放行截图工具**：解除防截图限制，在录制视频或使用各类截图工具截取屏幕时，按键回显能被完美截取并纳入课件与教程中。

### 3. 托盘控制中心 (Tray Control Center) —— 2×3 对称快捷开关
- **一键掌控全局工具**：托盘菜单全新引入 2×3 对称快捷胶囊网格，鼠标特效（聚光灯）与对话框增强一键秒级启闭；
- **动态高度贴合与无缝重启**：托盘窗口根据内容自适应伸缩，修改核心配置后提供丝滑的重启提醒与一键热重载。

### 4. 开机自启动与权限管理 —— 稳定无感，免 UAC 烦扰
- **开机自启更稳定、隔离更彻底**：采用 Windows 原生任务计划机制为当前登录用户建立专属隔离通道，开机自启稳定可靠；
- **安装包开机自启选项**：安装包在向导中新增「开机自动启动 EasyTools」勾选项，无论安装还是卸载均能智能同步配置；
- **免 UAC 弹窗**：支持以最高权限开机静默启动，彻底摆脱每次开机都被 Windows 弹窗询问管理员权限的困扰。

### 5. 全局排版与无边框窗口 —— 细节质感飞跃
- **12px ClearType 次像素字字锐利**：重构全局排版令牌，全面消除全界面字体发虚、单薄与视觉割裂感；
- **全面拥抱矢量图标 (零 Emoji 规范)**：告别简陋的彩色 Emoji，全界面升级为高精度 Lucide 矢量 SVG 图标与玻璃拟态微胶囊；
- **设置窗口 8 方向自由拉伸**：无边框设置窗口全面支持四周及四角的流畅拖拽缩放，想放大看细节或缩小靠边放，随心所欲；
- **设置保存全局居中 Toast**：修改设置后屏幕居中弹出微型胶囊 Toast，1.8 秒后优雅淡出，再也不会出现 Toast 偶发卡死常驻的缺陷。

### 6. 全屏游戏与视频免打扰 (全模块标配)
- 鼠标聚光灯、按键回显、鼠标手势与快速文件搜索，全模块标配全屏独占检测；
- 当你在玩 3D 全屏游戏或全屏看电影时，所有工具自动静默放行，零打扰、零误触。

---

## 🌟 v1.0.1 官方正式版发布说明

### 1. 全新推出：文件对话框智能助手插件 (Plugin_DialogEnhancer)
- **跨应用独立路径记忆与智能回位**：自动记住不同应用（Word、Excel、VS Code、Chrome 等）上一次打开或保存的目录，再次弹出文件对话框时瞬间自动定位，大幅削减反复层层翻找路径的时间；
- **智能路径归一化引擎**：选择文件时精准记忆其所属父目录，多选文件时回落当前层级，文件夹选择器停留在当前选定目录，杜绝路径漂移；
- **资源管理器当前目录同频 (Quick Switch)**：在文件对话框顶部悬浮快捷工具条，一键直达当前正在活跃浏览的 Windows 资源管理器文件夹；
- **常用工作区与最近使用收藏**：支持将常用代码仓库或办公文件夹置顶收藏，在所有文件对话框中一键秒级跳转；
- **UIAutomation 可见性防竞争校验**：智能识别当前可见的 Address Band 面包屑，彻底消除原生隐藏输入框残留旧路径的数据竞争。

### 2. 高刷屏与高报告率手势优化 (Plugin_Gesture)
- **高刷新率手势渲染节流**：在 500Hz / 1000Hz 高回报率电竞鼠标与 120Hz/144Hz/240Hz 高刷屏下，动态合并渲染帧并稳定节流至 120Hz，轨迹如丝般顺滑，大幅降低 CPU 开销；
- **高分屏（High-DPI）坐标完美对齐**：针对 125%/150%/175%/200% 缩放自适应修正，彻底消除缩放环境下的手势跳点与偏移；
- **规范窗口扩展样式与任务栏保护**：手势轨迹与提示覆盖层引入 WS_EX_TOOLWINDOW 与无激活属性，杜绝在 Windows 任务栏产生多余的幽灵图标占位。

### 3. 极速响应与托盘首开优化
- **托盘菜单后台静默预热**：系统启动后在后台提前就绪托盘窗口，首次右键或左键点击瞬间呈现，彻底消除白屏与首开 Loading 延迟；
- **自动化运维与 CLI 脚本**：新增官方一键静默安装与卸载脚本（install.ps1/cmd、uninstall.ps1/cmd），支持无人值守分发。

---

## 核心架构与系统特性

### 1. 极致性能与低开销运行时
- **C++20 原生高性能内核**：核心引擎完全采用 C++20 编写，消除中间层运行时开销与垃圾回收（GC）停顿；
- **冷热分离物理内存修剪（Working Set Trimming）**：
  - 严守「冷路径退场修剪，热操作期间绝不修剪」原则；
  - 在截图完成、录屏停止、OCR 识别结束或窗口隐藏等生命周期终端，自动释放大尺寸位图缓冲区并主动归还物理内存至操作系统；
  - 严禁在 1000Hz 鼠标钩子、60FPS 渲染循环或键盘连击流等热路径触发修剪，从根源杜绝软缺页卡顿；
- **DirectComposition 硬件合成加速**：手势轨迹与 HUD 浮窗原生接入 DirectComposition 硬件合成渲染管线，在高刷屏与多显示器环境下彻底消除画面撕裂与帧延迟；
- **High-DPI 与全屏缩放完美自适应**：全面覆盖 Per-Monitor DPI Awareness V2，底层启用物理像素精准对齐，在 Windows 各级缩放下字字锐利。

### 2. 模块化原生插件体系（Plugin Architecture）
- **ABI 稳定型 C 导出接口**：支持功能模块物理隔离与动态热插拔；
- **独立配置隔离**：每个插件拥有专属配置模型与生命周期管理，单模块崩溃不影响主进程稳定运行。

---

## 核心功能矩阵与易用性设计

### 1. 全局智能鼠标手势（Plugin_Gesture）
- **抗倾斜磁吸判定算法**：引入基准方向磁吸区容差与连续折线拟合算法，彻底解决绘制「下」手势轻微左斜被误判为「下-左」组合的痛点；
- **穿透式目标窗口命中测试**：重构覆盖层命中测试与 Z-Order 避让机制，手势触发的快捷键与窗口控制 100% 精准投递至光标下方的实际目标应用；
- **单窗口与复杂框架智能关窗兜底**：针对单标签页浏览器、CEF、Qt 等无损关窗场景，智能执行层级升格或平滑回退至 Alt+F4；
- **全屏免打扰放行机制**：检测到大型 3D 游戏或全屏独占生产力软件时，自动放行右键操作；
- **丰富动作矩阵**：支持多达数十种预设指令，涵盖网页前后导航、标签页切换、窗口最大化/最小化/关闭/置顶、全屏切换、全局多媒体播放控制、截图与即时搜索。

### 2. 多合一高能截屏、长截图与录屏（Plugin_Capture）
- **DXGI / D3D11 桌面复用极速抓取**：毫秒级首帧捕获，支持多显示器跨屏连续截取；
- **智能边缘吸附与窗口嗅探**：自动感知当前鼠标悬浮的顶级窗口与子控件边界；
- **特征匹配无缝长截图**：高精度拼接算法，向下滚动时实时比对帧重叠区并平滑缝合；
- **硬件加速高清录屏**：原生支持 H.264、H.265 (HEVC) 与 WebM VP9 编码，集成 WASAPI 扬声器回环音频采集；
- **会话级热键动态武装（HotkeyArmScope）**：录屏暂停/继续专属快捷键（默认 Ctrl+Shift+P）仅在录制会话活跃时武装，日常状态自动释放，彻底杜绝与 VS Code、Cursor 等代码编辑器命令面板的快捷键冲突；
- **离线 / 在线双模 OCR 文字识别**：截屏后一键离线提取文字与表格数据，保护隐私安全。

### 3. NTFS 毫秒级极速全盘搜索（Plugin_Search）
- **底层 USN Journal / MFT 解析**：直接读取 NTFS 主文件表，数百万文件秒级索引建立与增量监控；
- **普通权限平滑降级（SCM Fallback）**：非管理员权限启动时，若无法调用系统服务控制器，自动平滑降级为便携索引引擎，开箱即用；
- **虚拟化长列表渲染**：前端虚拟滚动支撑十万级搜索结果实时流式展示，按键响应低至亚毫秒。

### 4. 屏幕按键回显（Plugin_Keycast）
- **低延迟无感 HUD 悬浮窗**：实时捕捉并以现代磨砂质感显式展示键盘击键组合；
- **细粒度视觉定制**：支持自定义字体大小、背景透明度、动效淡出时长与屏幕显示位置。

### 5. 即时文件快速预览（Plugin_QuickLook）
- **空格键一键预览**：无需打开庞大的专用软件，按空格键即可瞬间预览 Markdown、源码、高清图片、音视频、PDF 与压缩包内容。

### 6. 人机工程学设置中心与视觉体验
- **现代 Fluent / Mica 磨砂玻璃质感**：基于 Chromium WebView2 构建的顶级响应式用户界面，支持暗黑模式、明亮模式与系统主题跟随；
- **舒适黄金比例默认尺寸**：设置窗口采用 1100x750 舒适默认尺寸，配合智能屏幕自适应居中，消除压迫感；
- **以管理员身份运行与 UAC 平滑重启联动**：通用设置提供管理员运行开关，点击后即可联动 Windows UAC 提权并无缝重启当前实例；
- **优雅启动微反馈**：程序启动就绪后于屏幕底部中央以高反差白边悬浮 Toast 优雅提示，零焦点抢占，倒计时结束后即时清爽关闭。

---

## 校验与资产信息

* **项目主页**：https://github.com/yuan278501381/easyTools
* **原作者**：Yy1 (GitHub yuan278501381) - https://github.com/yuan278501381
* **开源许可证**：MIT License (Copyright (c) 2026 Yy1 (GitHub yuan278501381) <https://github.com/yuan278501381> & EasyTools contributors)
