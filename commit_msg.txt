feat: 截图重构与交互进阶（放大镜取色、选区调整、玻璃特效）

业务角度：
1. 取色与对齐升级：新增带十字准星的像素级取色放大镜，支持按 C 键一键提取光标处 HEX 颜色值并给出 UI 复制提示。
2. 选区灵活二次调整：打破传统截图只能框一次的限制，支持通过拖拽控制点八向缩放选区，或拖拽边框整体移动选区。
3. 现代玻璃拟物态 UI：为工具栏悬浮提示、尺寸标签等模块引入高级的磨砂玻璃透视面板效果，彻底消除粗糙的纯色底。
4. 细节与稳定增强：优化了撤销/重做和坐标体系之间的映射（选区重定界后自动平移所有标注坐标以钉住原位）。

技术角度：
1. 性能深度优化：引入脏标记机制 (m_needsRender) 拦截无效的全屏无脑重绘；同时实现基于 ID2D1Bitmap 缓存的标注重绘分离 (m_markupCacheDirty)，消灭了性能刺客问题。
2. 架构稳健度：为 CaptureOverlay 窗口过程加入了全局 try...catch 防护网，避免异常抛出导致 Win32 消息循环崩溃。
3. 底层图层绘制：玻璃效果借由 D2D1Layer 和圆角几何裁剪进行多层混合渲染，大幅增强了 C++ 桌面端界面的高级质感。

（注：这些是Opus4.8 Extra 没做完的）
